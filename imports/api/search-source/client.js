const fields = [ 'packageName', 'description' ];
const options = {
  keepHistory: 1000 * 60 * 5,
  localSearch: true
};

//export default SearchTool = new SearchSource( 'the-search', fields, options );